export const enum Gateways {
  sendMessage = "send_message",
  
  disconnect = "room_disconnect",
  disconnectMany = "rooms_disconnect",
  disconnectAll = "disconnect_all",
  
  connect = "room_connect",
  connectMany = "rooms_connect",

  receiveMessage = "receive_message"
}